extends Resource
class_name MatchMode

export (String) var name: String
export (String, MULTILINE) var description: String
export (bool) var requires_goals := false
export (PackedScene) var manager_scene: PackedScene
export (PackedScene) var config_scene: PackedScene
